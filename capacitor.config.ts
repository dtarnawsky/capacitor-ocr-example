import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.ocr.example',
  appName: 'capacitor-ocr-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
